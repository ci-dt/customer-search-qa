dependencies:
  override:
    - mvn -s circleci.settings.xml -DskipTests clean install

test:
  override:

deployment:
  main:
    branch: master
    commands:
      - mvn -s circleci.settings.xml -DskipTests deploy